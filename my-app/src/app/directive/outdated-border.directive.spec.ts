import { OutdatedBorderDirective } from './Outdated-border.directive';

describe('OutdatedBorderDirective', () => {
  it('should create an instance', () => {
    const directive = new OutdatedBorderDirective();
    expect(directive).toBeTruthy();
  });
});
